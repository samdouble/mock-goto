{
  "name": "mock-goto-examples",
  "private": true,
  "description": "Runnable examples for mock-goto",
  "scripts": {
    "test": "mocha --require ts-node/register --check-leaks --timeout 30000 '*.test.ts'"
  },
  "dependencies": {
    "mock-goto": "file:.."
  },
  "devDependencies": {
    "@types/mocha": "^10.0.3",
    "@types/node": "^26.1.0",
    "chai": "^6.2.2",
    "mocha": "^11.7.6",
    "playwright": "^1.52.0",
    "puppeteer": "^25.4.0",
    "ts-node": "^10.9.1",
    "typescript": "npm:@typescript/typescript6@^6.0.2"
  }
}
